server:
  port: 8083

spring:
  application:
    name: tripservice

  datasource:
    url: jdbc:postgresql://localhost:5432/rideshare_trips
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true